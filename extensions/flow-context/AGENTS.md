# DOX — extensions/flow-context

Files in this area. Purposes left for the agent to author.

| `index.ts` | activate(pi): flow_results tool (list/summary/agent/runs via projectRuns); /flows, /flows:delete, /flows:generate; consumes flow:delete-request, emits flow:rediscover; reads .pi/flows/results. |
